import { describe, expect, it } from "@jest/globals";
import { getSeveralAudioFeatures } from "./spotify-api";

describe(getSeveralAudioFeatures.name, () => {
  it("should get chunks of audio features", () => {
    expect(true).toBeTruthy();
  });
});
