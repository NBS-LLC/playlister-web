{
  "name": "playlister-web",
  "version": "1.0.0",
  "description": "The Web Version of Playlister",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "format": "prettier . --check",
    "lint": "eslint .",
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "",
    "build": "npm-run-all clean format lint compile dist --print-label",
    "watch": "tsc --watch"
  },
  "author": "Nick Derevjanik",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "@eslint/js": "^8.56.0",
    "eslint": "^8.56.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.3.0",
    "rimraf": "^5.0.7",
    "typescript": "^5.4.5",
    "typescript-eslint": "^7.11.0"
  }
}
